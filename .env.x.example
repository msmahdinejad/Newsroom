# Optional X authentication for the isolated social collector.
#
# Copy this file to `.env.x.local`. The local file is ignored by Git and is
# loaded only by the Agent-Reach worker. Use a dedicated operational account.
# Never pass these values as command-line arguments or commit the local file.

TWITTER_AUTH_TOKEN=
TWITTER_CT0=
