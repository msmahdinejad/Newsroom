# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
.uv/
*.egg-info/
dist/
build/

# Environment
.env
.env.local
.env.x.local
.env.providers.local
!.env.example
!.env.providers.example

# IDE
.vscode/
.idea/
*.swp
*.swo

# Database
*.db
*.sqlite3

# Logs
*.log
logs/

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# OS
.DS_Store
Thumbs.db
backups/

# Telegram MTProto sessions (Gate 3) — never commit, never bake into images
data/sessions/
*.session
*.session-journal
*.session-shm
*.session-wal

# Gate 5: Agent-Reach worker local state — cookies, tokens, upstream tool installs.
# Never commit, never bake into images, never share with editorial/Telegram containers.
data/agent-reach/
.agent-reach-venv/
agent_reach_audit/

# Gate 6: source workbook (source data + derived import copy).
# The importer copies the original to config/import/source-radar.xlsx on import.
tech_ai_programming_source_radar_global_2026*.xlsx
config/import/
